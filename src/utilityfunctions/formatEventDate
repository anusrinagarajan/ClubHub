function formatEventDate(start_time) {
    const date = new Date(start_time);
    return (
      date.toLocaleDateString(undefined, {
        weekday: "short",
        month: "short",
        day: "numeric",
      })
    );
}

export default formatEventDate;